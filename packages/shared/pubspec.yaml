name: industrynight_shared
description: Shared models and API client for Industry Night apps
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  http: ^1.1.0
  json_annotation: ^4.8.1
  flutter_secure_storage: ^9.0.0
  equatable: ^2.0.5

dev_dependencies:
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  test: ^1.24.9
